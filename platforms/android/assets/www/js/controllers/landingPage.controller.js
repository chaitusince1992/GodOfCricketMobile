questionApp.controller('landingPageController', ['$scope', '$rootScope','$location','commonConstants', 'commonServices',
    function ($scope,$rootScope,$location, commonConstants, commonServices) {
        var self = $scope;
        console.log('inside login controller');
        self.init = function () {
        }
    }
]);
